# BlytzHire Platform
# Use specific compose files for different services:
# PostgreSQL: docker compose -f docker-compose.postgres.yml up
# All services: docker compose -f docker-compose.all.yml up (when ready)

version: '3.8'

services:
  # Placeholder for future main compose file
  # Individual services are in separate compose files:
  # - docker-compose.postgres.yml (Database)
  # - docker-compose.backend.yml (API)
  # - docker-compose.frontend.yml (Web app)

volumes:
  # Volumes are defined in service-specific compose files

networks:
  # Networks are defined in service-specific compose files
